#include<stdio.h>
int main(){
		int x, y; 
		printf("enter the value of x");
		scanf("%d",&x);
		printf("enter the value of y");
		scanf("%d",&y);
		if{
			(x>y)\\print x otherwise print y}
			(x<y)\\print 0
			
			}
			return 0;
}
